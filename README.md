### build the library using the Rollup script command:

#### `npm run rollup`

### To test the stories, run the following command:

#### `npm run storybook`

### run the test with this command:

#### `npm run test`
